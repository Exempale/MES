# MES — Message Encrypted Secure

**Анонимный шифрованный мессенджер с end-to-end encryption**

![MES](https://img.shields.io/badge/version-0.3.0-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## 🌟 Основные возможности

### 🔐 Безопасность и шифрование
- **End-to-end encryption** с использованием AES-GCM (256 бит)
- **PBKDF2** для деривации ключа (200,000 итераций)
- **Ключ и соль хранятся в URL** (не передаются на сервер)
- **Демо-уровень безопасности** - подходит для неформального общения
- **Сервер только ретранслирует** зашифрованные данные

### 💬 Функционал мессенджера
- **Текстовые сообщения** с поддержкой никнеймов
- **Отправка файлов** любого размера с чанкованием (256 КБ чанки)
- **Изображения** с предпросмотром в чате
- **Голосовые сообщения** с эффектами (робот, высокий/низкий голос)
- **Индикатор набора текста** в реальном времени
- **Счетчик онлайн-пользователей** в комнате

### 🎨 Пользовательский интерфейс
- **Адаптивный дизайн** для мобильных и десктопных устройств
- **Темная/светлая тема** с переключателем
- **Прогресс-бары** для загрузки файлов
- **Локальное сохранение истории** (IndexedDB)
- **Интуитивный интерфейс** на русском языке

### 🏠 Управление комнатами
- **Создание комнат** с уникальными ID
- **Ссылки-приглашения** с встроенными ключами шифрования
- **Завершение чата** с очисткой истории
- **Анонимность** - без регистрации

## 🚀 Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк
- **WebSockets** для real-time коммуникации
- **Python 3.8+** для серверной части

### Frontend
- **Vanilla JavaScript** (ES6+) - без зависимостей
- **CSS3** с CSS-переменными для темизации
- **Web Crypto API** для клиентского шифрования
- **IndexedDB** для локального хранения

### Безопасность
- **AES-GCM** для симметричного шифрования
- **PBKDF2** для деривации ключей
- **Base64URL** для безопасной передачи данных

## 📦 Установка и запуск

### Требования
```bash
Python 3.8+
pip
```

### Установка зависимостей
```bash
pip install fastapi uvicorn python-multipart websockets
```

### Запуск сервера
```bash
# Стандартный запуск
python app.py

# Или с указанием порта
PORT=3000 python app.py

# Для разработки с автоперезагрузкой
uvicorn app:app --host 127.0.0.1 --port 8000 --reload
```

### Доступ
Приложение будет доступно по адресу:
- `http://127.0.0.1:8000` (по умолчанию)
- `http://127.0.0.1:8000/room/{room_id}` для конкретной комнаты

## 🎯 Как использовать

### 1. Создание комнаты
1. Откройте приложение в браузере
2. Нажмите **"Создать комнату"**
3. Опционально введите никнейм
4. Система автоматически сгенерирует зашифрованную ссылку

### 2. Приглашение участников
1. Нажмите **"Скопировать приглашение"**
2. Отправьте ссылку другим участникам
3. Ключ шифрования встроен в ссылку после `#`

### 3. Общение
- **Текст**: вводите сообщения и нажимайте Enter или "Отправить"
- **Файлы**: перетащите файлы в чат или используйте кнопку 📎
- **Голос**: нажмите 🎙️ для записи голосового сообщения
- **Эффекты**: выберите эффект голоса перед записью

### 4. Завершение чата
Нажмите **"Завершить чат"** для очистки истории и выхода из комнаты

## 🔧 Структура проекта

```
MES_Pro_Plus/
├── app.py              # FastAPI сервер с WebSocket
├── static/
│   ├── index.html      # Основной HTML интерфейс
│   ├── mes.js          # Клиентская логика и шифрование
│   └── style.css       # Стили и темы
└── README.md           # Документация
```

## 🛡️ Архитектура безопасности

### Модель угроз
- **Сервер не имеет доступа** к расшифрованным сообщениям
- **Ключи никогда не покидают** клиентское устройство
- **История хранится локально** до завершения чата
- **Анонимность** - нет привязки к личным данным

### Ограничения безопасности
- **Демо-уровень** - не для критически важных данных
- **Отсутствие проверки подлинности** участников
- **Возможность MITM** при передаче ссылки
- **Нет perfect forward secrecy**

## 🔄 API

### WebSocket Endpoint
```
ws://127.0.0.1:8000/ws/{room_id}
```

### Формат сообщений
```javascript
// Зашифрованный пакет
{
  "iv": "base64url-encoded-iv",
  "ct": "base64url-encoded-ciphertext"
}

// Контрольные сообщения
{
  "_control": "online",
  "count": 3
}
```

### HTTP Endpoints
- `GET /` - главная страница
- `GET /room/{room_id}` - страница комнаты
- `GET /static/*` - статические файлы

## 🎨 Кастомизация

### Изменение порта
```python
# В app.py
port = int(os.environ.get("PORT", "8000"))  # Измените 8000 на нужный порт
```

### Настройка CORS
```python
# В app.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Ограничьте домены для продакшена
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Темы оформления
```css
/* В style.css */
:root {
  --bg: #0b1020;           /* Фон */
  --accent: #3a86ff;       /* Акцентный цвет */
  --text: #e6e9f5;         /* Текст */
  /* ... другие переменные */
}
```

## 🚀 Разработка

### Локальная разработка
```bash
# Запуск с отладкой
uvicorn app:app --reload --host 127.0.0.1 --port 8000

# Отслеживание изменений в static/
python app.py  # Автоматическая перезагрузка включена
```

### Тестирование
```bash
# Тестирование WebSocket
wscat -c ws://127.0.0.1:8000/ws/test-room

# Проверка статических файлов
curl http://127.0.0.1:8000/static/style.css
```

## 📝 TODO

- [ ] WebRTC для видеозвонков
- [ ] Улучшенная аутентификация участников
- [ ] Perfect Forward Secrecy
- [ ] Мобильное приложение
- [ ] Административная панель
- [ ] Статистика использования

## 🤝 Contributing

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## ⚠️ Дисклеймер

**MES Pro+ предназначен для неформального общения и не гарантирует промышленного уровня безопасности.** Не используйте для передачи критически важной информации.

## 📞 Контакты

- **Проект**: MES Pro+ - Message Encrypted Secure
- **Версия**: 0.3.0
- **Статус**: Active Development
